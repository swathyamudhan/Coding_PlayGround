package com.codingbat.map2;

public class WordCountTest {
	@org.testng.annotations.Test
	public void Test() {
		WordCount map = new WordCount();
		// assertTrue(Maps.difference({"a": 2, "b": 2, "c": 1}, ["a", "b", "a", "c",
		// "b"]).areEqual());
		// assertEquals(["a", "b", "a", "c", "b"],new HashMap<String,Integer>() {"a": 2,
		// "b": 2, "c": 1});

	}
}
